# Agent Rules

- **Firestore Rules Reminder**: When generating code that creates new collections or documents in Firestore, proactively remind the user to update the Firestore rules to ensure proper permissions are in place.
